<!-- walnut:generated -->

# Bominso_1 — Walnut Project Context

> Auto-generated by the Walnut extension from this project's Walnut context.
> Manual edits will be overwritten on the next link/refresh. To take ownership
> of this file, delete the `walnut:generated` marker on the first line.

**Project:** Bominso_1
**Repository:** Walnut-Atib-methods

## Overview

# Project Overview

Bominso_1 is a single-repo project consisting of **Walnut-Atib-methods**, a TypeScript library of reusable automation helper functions built as a plugin or extension for the "Walnut" test automation platform. The library is not a standalone application — it has no HTTP server, CLI entry point, or build output — and is intended to be imported and invoked by an external Walnut test runner that injects a shared `WalnutContext` execution object into each function.

The primary tech stack is TypeScript, with two notable runtime dependencies: `@playwright/mcp` (a Playwright Model Context Protocol integration for browser automation) and `ssh2` (for SSH connectivity to remote servers). There are no detected web frameworks or external SaaS integrations beyond these two libraries. The 19 source files are all flat inside the `walnut-methods/` directory, each exporting a single async function.

The project appears to target QA engineers and test automation developers working against a Temenos T24/Transact core banking system. The presence of `executeSshT24` and `buildOfsScript` functions — which send OFS (Open Financial Services) messages to a T24 server over SSH — is the strongest signal of the target domain.

## System Architecture

# System Architecture

This is a single-repo project — update once more repos complete.

**Walnut-Atib-methods** plays the role of a **shared library / plugin**:

- It exports 19 TypeScript async functions, each accepting a single `WalnutContext` argument injected by the Walnut runtime at call time.
- It has no HTTP server, no CLI entry point, and no declared build script, meaning it is consumed directly by a parent Walnut test runner rather than deployed independently.
- The `@playwright/mcp` dependency connects it to a Playwright-based browser automation layer (likely managed by the Walnut runtime), while the `ssh2` dependency enables direct SSH sessions to remote servers (specifically a Temenos T24 banking backend, based on function names).
- All source files live flat under `walnut-methods/`, with no internal sub-module boundaries or inter-file dependency graph documented in the available facts.

There are no other repos in this project at this time, so no cross-repo connections (shared types, API consumption, auth handshakes) can be described.

## Domain

# Domain
Inferred from code only — may be inaccurate for early-stage or generic projects.

This project provides a library of reusable test automation helpers for QA engineers writing Walnut-based test scripts against a Temenos T24/Transact core banking application. The most distinctive functions — `executeSshT24` (in `sendOFS.ts`) and `buildOfsScript` — send OFS (Open Financial Services) messages to a T24 server over SSH, which is a standard integration pattern for T24 test automation and data setup.

Beyond T24-specific operations, the library covers general-purpose automation utilities: DOM interaction (`getDomTextAndStore`, `getShadowDomAttributeByXpath`, `setAttributeByXpath`, `sumAmountsByXpath`), string manipulation (`concatenateValues`, `replaceInString`, `splitByDelimiter`, `trimValue`), date handling (`enterTodaysDate`), arithmetic (`arithmeticOperation`), assertions (`assertEqual`, `compareValues`), and in-browser JavaScript execution (`executeJsQuery`). This breadth suggests the library is intended as a comprehensive utility layer for end-to-end test scripts that interact with both the T24 web UI (via Playwright) and the T24 server backend (via SSH/OFS).

## Metadata

```json
{
  "project_id": "6a27feee6a96032dfd6fe580",
  "project_name": "Bominso_1",
  "generated_at": "2026-08-13T12:55:50Z",
  "llm_model": "claude-sonnet-4-6",
  "total_repos": 1,
  "total_loc": 1445,
  "language_distribution": {
    "Walnut-Atib-methods": 1445
  },
  "primary_languages": [
    "TypeScript"
  ],
  "all_frameworks": [],
  "all_external_integrations": [],
  "repos": [
    {
      "repo_id": "github_Balaji93476/Walnut-Atib-methods_1786625594437_h72hpymu4",
      "repo_name": "Walnut-Atib-methods",
      "primary_language": "TypeScript",
      "architecture_md": "# Architecture\n\nThis project is a TypeScript library of reusable automation helper functions, each living in its own file under the `walnut-methods/` directory. There is no detected web framework, HTTP server, or CLI entry point — the codebase appears to be a collection of standalone async functions intended to be imported and invoked by an external test automation or workflow orchestration system called \"Walnut\" (inferred from the `WalnutContext` type that every function accepts as its sole argument).\n\nThe two runtime dependencies are `@playwright/mcp` (a Playwright Model Context Protocol integration, suggesting browser automation is involved) and `ssh2` (for SSH connectivity). This combination points to a library that drives both browser interactions and remote server commands within a unified automation context. Every function follows the same signature — `async function \u003cname\u003e(ctx: WalnutContext)` — which is a strong indicator that `WalnutContext` is a shared execution context object injected by the Walnut runtime, carrying browser state, variables, and configuration.\n\nThe repository contains 19 TypeScript source files and 4 JSON files, all flat inside `walnut-methods/`. There is no build script or entry point declared in `package.json`, which suggests this library is consumed directly by a parent project or test runner rather than being executed standalone. The `@types/ssh2` dev dependency confirms TypeScript type-checking is used during development.\n",
      "domain_md": "# Domain\nInferred from code only — may be inaccurate for early-stage or generic projects.\n\nThis project appears to be a plugin or extension library for the \"Walnut\" test automation platform, providing reusable custom methods for automated testing of web applications — likely in the banking or financial services sector. The presence of `buildOfsScript` and `executeSshT24` (where \"OFS\" and \"T24\" are well-known terms in Temenos T24/Transact core banking software) strongly suggests the target application under test is a T24 banking system. The `executeSshT24` function in `sendOFS.ts` appears to send OFS (Open Financial Services) messages to a T24 server over SSH, which is a standard integration pattern for T24 automation.\n\nThe likely users of this library are QA engineers or test automation developers who write Walnut-based test scripts against a T24 banking application. The helper methods cover a range of utility needs: DOM interaction (`getDomTextAndStore`, `getShadowDomAttributeByXpath`, `setAttributeByXpath`, `sumAmountsByXpath`), string manipulation (`concatenateValues`, `replaceInString`, `splitByDelimiter`, `trimValue`), date handling (`enterTodaysDate`), arithmetic (`arithmeticOperation`), assertions (`assertEqual`, `compareValues`), and JavaScript execution in the browser (`executeJsQuery`).\n",
      "top_level_dirs": [
        "walnut-methods"
      ],
      "total_loc": 1445
    }
  ]
}
```

---
_Source: Walnut code graph / project context. Treat this as background context — verify against the code before relying on it._
